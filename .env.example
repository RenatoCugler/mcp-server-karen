# ============================================================================
# Karen MCP Server - Environment Configuration
# ============================================================================
# 
# This is an EXAMPLE file. To use it:
# 1. Copy this file: cp .env.example .env
# 2. Fill in your actual values in .env
# 3. NEVER commit .env to git (it's in .gitignore)
#
# ============================================================================

# ============================================================================
# OPENAI API CONFIGURATION (Optional)
# ============================================================================
# Get your API key from: https://platform.openai.com/api-keys
# The server works WITHOUT this (uses fallback responses)
# With API key: Dynamic, creative AI-generated Karen responses
# Without API key: Pre-written funny responses

OPENAI_API_KEY=sk-your-openai-api-key-here

# Which OpenAI model to use
# Options: gpt-3.5-turbo (faster, cheaper), gpt-4 (smarter, funnier)
# Default: gpt-3.5-turbo
OPENAI_MODEL=gpt-3.5-turbo

# ============================================================================
# IMGFLIP API CONFIGURATION (Optional - For Meme Generation)
# ============================================================================
# Create a free account at: https://imgflip.com/signup
# The meme generator works WITHOUT credentials (uses default account)
# With credentials: Higher rate limits and personalized memes
# Get credentials at: https://imgflip.com/api

IMGFLIP_USERNAME=your-imgflip-username
IMGFLIP_PASSWORD=your-imgflip-password

# ============================================================================
# DOCKER MCP SECRETS (Alternative to .env)
# ============================================================================
# You can also set these using Docker MCP secrets:
#
# docker mcp secret set OPENAI_API_KEY="sk-your-key-here"
# docker mcp secret set OPENAI_MODEL="gpt-3.5-turbo"
# docker mcp secret set IMGFLIP_USERNAME="your-username"
# docker mcp secret set IMGFLIP_PASSWORD="your-password"
#
# Docker secrets are recommended for production!
# ============================================================================

# ============================================================================
# DEVELOPMENT SETTINGS
# ============================================================================
# Logging level: DEBUG, INFO, WARNING, ERROR
# DEBUG = verbose logging, INFO = normal
LOG_LEVEL=INFO

# API timeout in seconds
# How long to wait for OpenAI responses before falling back
API_TIMEOUT=30

# ============================================================================
# NOTES
# ============================================================================
# - This file is for local development only
# - Use Docker MCP secrets in production
# - Never share your API keys
# - Free tier OpenAI has rate limits
# - Server works great without OpenAI (fallback mode)
# ============================================================================
